# 01-validation-broadcast-foundation-shell.md

Validation of [`01-spec-broadcast-foundation-shell.md`](./01-spec-broadcast-foundation-shell.md)
against [`01-tasks-broadcast-foundation-shell.md`](./01-tasks-broadcast-foundation-shell.md).

Branch: `feat/broadcast-foundation-shell` @ `e758dd9` · Base: `main`

---

## 1) Executive Summary

**Overall: FAIL** — one gate tripped, on one narrowly scoped item.

| Gate | Result | Note |
| --- | --- | --- |
| A — no CRITICAL/HIGH | **TRIPPED** | One HIGH: FR U2-7 (safe-area insets) has no evidence of the behavior it claims. |
| B — no `Unknown` FRs | **TRIPPED** | FR U2-7 is Partial: implementation present, behavior never observed. |
| C — proof artifacts accessible | PASS (with note) | All committed artifacts exist and check out. Waived image artifacts have working substitutes — verified independently, see §4. |
| D — file integrity | PASS | No unmapped core changes. One supporting file (`.claude/launch.json`) is unlisted but linked; MEDIUM, non-blocking. |
| E — repository standards | PASS | Plain CSS + single stylesheet, named-export TSX, `BASE_PATH` honored, conventional commits. |
| F — no secrets in artifacts | PASS | No credentials, tokens, invite codes, cookies, or account email in any artifact. Separate PII concern raised in §3. |

**Implementation Ready: No — but narrowly.** 14 of 15 functional requirements are
verified with evidence I re-ran or re-read myself; the single blocker is that the
tab bar's safe-area inset has never been observed resolving non-zero, on hardware
or in simulation, so FR U2-7 is asserted by code reading alone.

**Key metrics**

| Metric | Value |
| --- | --- |
| Functional requirements verified | **14 / 15 (93%)** — 1 Partial, 0 Failed |
| Proof artifacts working | **19 / 20 (95%)** — the safe-area browser check is the miss |
| Automated gates | `npm run test` 157/157 pass · `npm run build` exit 0 |
| Build vs baseline | No new TS errors, no new Vite warnings (the >500kB chunk notice is pre-existing in `build-baseline.txt`) |
| Files changed | 52 vs. 24 planned in Relevant Files — the excess is 9 screenshots + 7 proof/artifact docs, all supporting |

**On the owner's note ("skipped some proof artifacts but checked them myself
manually").** Taken at face value for the six image-only waivers in the task
list's waiver section (1.10, 2.10, 3.9, 4.12, 5.17, half of 6.5) — each also has
non-image substitute evidence, which I checked directly rather than accepting the
attestation alone. It does **not** cover sub-task 5.16: the task list itself
records that one as *not capturable on any available host*, with no manual check
claimed. That is the blocker below.

---

## 2) Coverage Matrix

### Functional Requirements — Unit 1: Broadcast Token System

| ID | Requirement | Status | Evidence |
| --- | --- | --- | --- |
| U1-1 | Retarget token *values*, retain every token name | **Verified** | `tokens.test.ts` asserts against `token-names.fixture.txt` (27 names captured at `a4d1956`); 157/157 pass on re-run. `git diff main...HEAD` touches no component for `9960d48`. |
| U1-2 | Dark is designed default: `#0e1116` ground, `#161b23` panels, `#f0a92c` amber | **Verified** | `styles.css:79-80` `--bg-page: #0e1116`, `--bg-card: #161b23`; `:101` `--accent: #f0a92c`. Amber active tab confirmed in the phone-width Season Stats capture (reviewed during validation; image withheld — see MED-1). |
| U1-3 | Light theme considered, accent darkened for legibility | **Verified** | `styles.css:48` `--accent: #926100`; `contrast-report.md` — 4.78:1 on `--bg-page`, 5.34:1 on `--bg-card`. Light ground `#f4f2ef` / cards `#ffffff` is a distinct treatment, not an inversion. |
| U1-4 | Condensed tabular numeric token, applied to table figures | **Verified** | `--font-numeric` + `.num` rule in `styles.css`; applied across 6 components. Visually confirmed during validation against the phone-width captures: Avg Finish column (5.10/5.40/6.10/6.40) and Box Score PTS column (24.12/29.50/9.10) align down the column. Images withheld — see MED-1. |
| U1-5 | Status tokens distinct from accent | **Verified** | `contrast.test.ts` CIELAB hue-separation case: success 70.8°, danger 47.7° from accent, floor 25°. Deviation from ratio-based separation is documented with reasoning in the task 1.0 proof; the substitution is sound — contrast ratio is a luminance metric and cannot express hue distinctness. |
| U1-6 | `ThemeContext` behavior preserved | **Verified** | `git diff main...HEAD -- frontend/src/contexts/ThemeContext.tsx` → **0 lines**. Runtime toggle + `data-theme` + `localStorage` persistence exercised live in task 6.7. |
| U1-7 | All pairings meet WCAG 2.1 AA in both themes | **Verified** | `contrast-report.md`: 124/124 pass, 0 fail, regenerated by the test run I executed. Structural border tokens are explicitly exempted with stated rationale; `--border-strong` (the control boundary) is held to 3:1 and measures 3.19:1. |

### Functional Requirements — Unit 2: Responsive App Shell

| ID | Requirement | Status | Evidence |
| --- | --- | --- | --- |
| U2-1 | Sidebar layout preserved ≥900px, collapse control intact | **Verified** | Task 4.11 measured 220px sidebar, collapse toggles to 36px. `responsive-matrix.md` records `.sidebar display: none` only below the breakpoint. |
| U2-2 | Single column + fixed bottom tab bar <900px | **Verified** | `styles.css:412` `@media (max-width: 900px)` shell block; `.tab-bar` fixed at `:225`. Bar present in all ten phone-width captures reviewed during validation. |
| U2-3 | All nine destinations; most-used direct, rest behind More | **Verified** | `navDestinations.ts` — 9 entries, 4 `primary` + 5 `overflow`; `navDestinations.test.ts` asserts count, tier split, path-to-router match, **and** the inverse (no route unreachable). See §3 LOW-1 on the 4-vs-5 wording. |
| U2-4 | Tab targets ≥44×44; others ≥24×24 | **Verified** | `touch-targets.md` — tab items 75×55 @375px, 64×55 @320px (binding case); sheet rows 375×48; league `select` 335×44 after the 30→44 fix. `styles.css:242` `min-height: 44px` on `.tab-bar-item`. |
| U2-5 | No `100vh` on layout/panes | **Verified** | `grep -n '100vh' frontend/src/styles.css` → **no matches**. Enforced by a `tokens.test.ts` case, so it also blocks regression in later specs. |
| U2-6 | No horizontal page scroll at 320px; wide content scrolls in-container | **Verified** | `responsive-matrix.md` 27/27 cells pass (9 routes × 320/375/768), real data, real values not just ✅. In-container proof: `.teams-wrap` 991px of content in a 351px box, 639.5px travel, page steady at 375/375. The Box Score capture showed the clipped-and-scrolling table with the page not overflowing. |
| U2-7 | Tab bar respects device safe-area insets | **Partial** ⚠️ | Code correct and precondition met: `index.html:5` carries `viewport-fit=cover`; `styles.css:234, 291, 440` use `env(safe-area-inset-bottom, 0px)`. **But** the value has only ever resolved to the `0px` fallback — desktop Chromium, no non-zero simulation, no hardware. The spec's own proof artifact for this ("with a non-zero `safe-area-inset-bottom` simulated, the tab bar's computed `padding-bottom` reflects the inset") was not produced. |
| U2-8 | Active destination marked with accent token | **Verified** | `styles.css:264-268` `.tab-bar-item.active` sets `color`, `border-top-color`, `background` from accent tokens. Measured `rgb(146, 97, 0)` on `/playoffs` with exactly one active tab; seen as amber "Season Stats" in the phone-width capture. |

### Repository Standards

| Area | Status | Evidence |
| --- | --- | --- |
| Styling (single `styles.css`, plain CSS, no framework) | **Verified** | One stylesheet, no preprocessor, no `@import`. New blocks carry comment headers (`/* Numerals */`, `/* Mobile shell */`). |
| Single breakpoint value | **Verified** | `grep '@media'` returns exactly two rules, both `max-width: 900px` — the pre-existing `.box-score-grid` (now `:1914`) and the new shell block (`:412`). No competing value introduced. |
| No font CDN | **Verified** | `grep -c '@import\|fonts.googleapis'` → 0 in both `styles.css` and `index.html`. Stack degrades to `ui-sans-serif`. |
| Components (React 18 fn components, TS, named exports) | **Verified** | `BottomTabBar.tsx`, `MoreSheet.tsx`, `navDestinations.ts` all named exports under `src/components/`. |
| Routing (`NavLink`, `BASE_PATH`) | **Verified** | Tab bar uses `NavLink`'s default `.active`; `navDestinations.test.ts` matches paths to routes registered in `main.tsx`. |
| Quality gates (`npm run build`, `npm run test`) | **Verified** | Re-run by this validation: build exit 0, 157/157 tests pass across 3 suites. |
| Testing scope discipline (Node env, no jsdom/RTL) | **Verified** | `vitest.config.ts` `environment: 'node'`, `include: ['src/**/*.test.ts']`. No `.tsx` test crept in. |
| Commits (conventional prefixes) | **Verified** | All eight spec commits use `test:`/`style:`/`feat:`/`docs:`. |

### Proof Artifacts

| Unit/Task | Artifact | Status | Verification result |
| --- | --- | --- | --- |
| 0.0 | `npm run test` green pre-change | Verified | Re-run today: 3 suites, 157 tests, exit 0, 201ms. |
| 0.0 | `artifacts/build-baseline.txt` + SHA | Verified | Present, headed `commit: a4d1956...`, exit 0 recorded. |
| 0.0 | `src/lib/token-names.fixture.txt` | Verified | 27 names, committed pre-change, consumed by `tokens.test.ts`. |
| 0.0 | Pre-change screenshots | **Waived** (0.2) | Owner-waived 2026-07-25, evidence permanently unrecoverable. Textual substitute (`git show a4d1956:styles.css`) accepted. |
| 1.0 | `tokens.test.ts` name-stability pass | Verified | Passes; deviation to superset + `ALLOWED_ADDITIONS` is correct — FR U1-1 constrains renames/removals, not additions. |
| 1.0 | Accent/status separation | Verified | Hue-separation case passes with recorded degrees. |
| 1.0 | Dark-theme screenshots | **Waived** (1.10) | Owner viewed live; withheld for third-party names. Substituted by `contrast-report.md`. |
| 2.0 | `contrast-report.md`, 124 pairings | Verified | Regenerated by my test run; 124 pass / 0 fail. |
| 2.0 | `ThemeContext.tsx` diff empty | Verified | Confirmed independently: 0 lines changed vs `main`. |
| 2.0 | Light-theme + persistence screenshots | **Waived** (2.10) | Runtime toggle and `localStorage` persistence verified live in 6.7 with computed values. |
| 3.0 | `fontVariantNumeric` browser check | Verified | Recorded across five table classes in the task 3.0 proof. |
| 3.0 | No font CDN | Verified | Re-run: `grep -c` → 0/0. |
| 3.0 | Numeral crop screenshot | **Waived** (3.9) | I confirmed the substitute myself during validation: the Season Stats and Box Score captures both show aligned tabular figures at full-table zoom. Images withheld post-validation — see MED-1. |
| 4.0 | `tokens.test.ts` viewport-unit case | Verified | Passes; independently confirmed no `100vh` remains anywhere in `styles.css`. |
| 4.0 | `viewport-fit=cover` in `index.html` | Verified | `index.html:5` re-read directly. |
| 4.0 | Shell screenshots at 1280/375/320 | **Waived** (4.12) | Substituted by `responsive-matrix.md` + the ten 306px captures. |
| 5.0 | `navDestinations.test.ts` | Verified | 8 assertions pass, including the inverse-reachability check added beyond the task text. |
| 5.0 | `touch-targets.md` | Verified | Every control at 375px and 320px measured; two real failures found and fixed (`select` 30→44px, `+ Add a league` 27→44px). Harness caveat stated openly in the doc. |
| 5.0 | **Safe-area inset browser check** | **Failed** | Never produced. Only the `0px` fallback has ever resolved. See §3 HIGH-1. |
| 6.0 | `responsive-matrix.md` 27 cells | Verified | 27/27 pass with real pixel values; two genuine overflow defects (`.hub-tabs` 448 vs 375, `.compare-controls .control` 334 vs 320) found, fixed, and the full matrix re-run rather than spot-checked. |
| 6.0 | Nine 375px screenshots | Verified (adapted) | Ten shots captured at **306px** — below the 320px floor, a stricter case than specified. I opened and read several directly during validation; all render correctly with the tab bar present. The images were withheld from the repository afterward — see MED-1 — with the readings preserved in `01-task-06-proofs.md`. |
| 6.0 | Navigation ≤2 taps | Verified | 4 primary = 1 tap, 5 overflow = More + item = 2 taps; enumerated in `responsive-matrix.md`. |
| 6.0 | `build-final.txt` vs baseline | Verified | Diffed: identical warning set. The >500kB chunk notice appears in **both**, so it is pre-existing, not introduced. |
| 6.0 | Sanitization check | Verified | My own scan for keys/tokens/passwords/invite codes across the spec directory returned only the grep commands in the proof docs themselves. No secrets. |

---

## 3) Validation Issues

| # | Severity | Issue | Impact | Recommendation |
| --- | --- | --- | --- | --- |
| HIGH-1 | **HIGH** | **FR U2-7 (safe-area insets) has no behavioral evidence.** Sub-task 5.16 waived as *not capturable* — no Xcode (`xcrun simctl` absent), simulator would not start, no device. The spec's Unit 2 proof artifact requires a browser check with a **non-zero** inset simulated; only the `0px` fallback has ever resolved. Evidence: `01-task-05-proofs.md:373`, and the task list's own "The one real gap" section, which states the inset "remains unverified." | Functionality. If the inset does not apply on hardware with a home indicator, the tab bar's targets sit under the indicator — the exact failure FR U2-7 exists to prevent. Risk is genuinely low (the CSS is the standard one-line pattern and `viewport-fit=cover` is confirmed present) but it is asserted, not shown. | Close it with either: (a) open the app on any iPhone with a home indicator and record `getComputedStyle(document.querySelector('.tab-bar')).paddingBottom` as non-zero; or (b) temporarily change the fallback to `env(safe-area-inset-bottom, 34px)` in a scratch build and confirm the computed padding tracks it — this proves the declaration consumes the variable, leaving only the platform's inset value unproven. Option (a) is a two-minute check and fully closes the requirement. |
| MED-1 | MEDIUM — **RESOLVED 2026-07-28** | **Screenshots exposing identifiable third parties' full names were removed before merge.** The ten PNGs are not committed to this public repository; the per-route observations read from them survive in `01-proofs/01-task-06-proofs.md`, and the waiver rationale in 1.10 / 4.12 is now applied consistently. Original finding follows. — **Committed screenshots expose identifiable third parties' full names.** `306-dark-season-stats.png` shows a full standings table of real people's first + last names; `306-dark-box-score.png` shows two more. This is the *same* concern the owner gave as the reason for withholding the desktop captures in 1.10 and 4.12 ("a league whose table exposes identifiable third parties' full names"). The repository therefore already contains what those waivers were protecting. Not a GATE F trip — the spec's Security Considerations enumerate session cookies, invite codes, account email, and devtools headers, none of which are present (`306-dark-account.png` shows only the username `ronan` and empty password fields). | Privacy, and internal consistency of the evidence record. | Decide one way and apply it uniformly: either accept league-member names in artifacts (and drop that rationale from 1.10/4.12), or mask the owner/team columns in the committed PNGs and restate the waiver reason. Leaving both positions in the record invites the question at review. |
| MED-2 | MEDIUM | **`.claude/launch.json` is an unlisted changed file.** Not in Relevant Files; added in `d1cbe72` alongside task 3.0's browser verification. Supporting tooling (dev-server launch config, no runtime effect), so GATE D2 rather than D1 — but the linkage lives only in commit adjacency, not in any task note. | Traceability. Verification is unaffected. | Add a one-line entry to Relevant Files noting it enables the browser verification used from task 3.8 onward. |
| LOW-1 | LOW | **FR U2-3 wording vs implementation: "five most-used surfaced directly."** The tab bar surfaces **four** destinations plus the More trigger in the fifth slot. Sanctioned — Resolved Open Question #1 records the decision as "Season Stats, Playoff History, Scoreboard, Team Hub, More" — and `navDestinations.ts:9-15` explains 4+More is what fits at 320px with 44px targets. The FR text was simply never amended. | None functionally; a reader diffing spec against code will pause. | Amend FR U2-3 to "four most-used surfaced directly in a five-slot bar," or add a pointer from the FR to the resolved question. |
| LOW-2 | LOW | **`touch-targets.md` sortable `<th>` rows are derived, not measured.** Marked `pending` in the doc and derived statically to ≈32×33px against the 24×24 floor. The doc is honest about this rather than papering over it. | Minor verification gap, comfortably above the floor. | Confirm opportunistically next time the authenticated app is open at 375px; not worth blocking on. |

**Not raised as issues, deliberately:** the six image-only waivers in the task
list's waiver section. Each has substitute evidence that I verified independently
rather than accepting on assertion, and in two cases (3.9, 6.5) the substitute is
arguably stronger than what was specified — a full-table render and a numeric
measurement of scroll travel, versus a crop.

---

## 4) Evidence Appendix

### Commands executed during this validation

```
$ git diff --stat main...HEAD              → 52 files, +6019 / -156
$ cd frontend && npm run test              → 3 suites, 157 tests, 0 fail, exit 0
$ cd frontend && npm run build             → tsc + vite build, exit 0, 775 modules
$ grep -n '100vh' frontend/src/styles.css  → no matches
$ grep -n '@media' frontend/src/styles.css → 2 hits, both max-width: 900px
$ grep -c '@import\|fonts.googleapis' …    → styles.css:0  index.html:0
$ grep -n 'safe-area-inset' …/styles.css   → :234 :291 :440, all env(…, 0px)
$ grep -n 'viewport' frontend/index.html   → viewport-fit=cover present
$ git diff main...HEAD -- …/ThemeContext.tsx → 0 lines
$ grep -rIn -E '(api[_-]?key|secret|password *[:=]|token *[:=]|invite[_-]?code|Bearer |sk-)' docs/specs/01-…/
                                           → 2 hits, both prose/grep-command text
```

### Commits analyzed

| SHA | Message | Maps to |
| --- | --- | --- |
| `07d0794` | `test: add vitest with contrast and token-stability suites` | Task 0.0 |
| `14fc478` | `docs: mark task 0.0 complete in task list` | Task 0.0 |
| `9960d48` | `style: retarget dark theme tokens to Broadcast palette` | Task 1.0 / FR U1-1, U1-2, U1-5 |
| `af39420` | `style: design Broadcast light theme and verify AA contrast` | Task 2.0 / FR U1-3, U1-7 |
| `d1cbe72` | `style: add condensed tabular numerals for statistical figures` | Task 3.0 / FR U1-4 |
| `ecc88c3` | `feat: make app shell responsive with svh units and 900px breakpoint` | Task 4.0 / FR U2-1, U2-2, U2-5, U2-6 |
| `261ed23` | `feat: add mobile bottom tab bar with More sheet` | Task 5.0 / FR U2-3, U2-4, U2-7, U2-8 |
| `d401273`, `92797bd`, `e758dd9` | `docs:` — verification artifacts, screenshots, waiver | Task 6.0 |

No commit is unrelated to the spec. The progression is coherent: infrastructure →
tokens → theme → type → layout → navigation → verification.

### Build comparison (Success Metric 5)

| | Baseline `a4d1956` | Final `e758dd9` |
| --- | --- | --- |
| Modules | 772 | 775 |
| CSS | 26.52 kB | 30.00 kB |
| JS | 607.45 kB | 611.53 kB |
| TS errors | 0 | 0 |
| Vite warnings | 1 (chunk >500kB) | 1 (chunk >500kB, **same**) |

No new error or warning. Growth is proportionate to three new components and the
mobile-shell CSS block.

### Artifacts inspected directly

`contrast-report.md` (124/124), `responsive-matrix.md` (27/27, plus two
documented fix cycles), `touch-targets.md`, `build-baseline.txt`,
`build-final.txt`, `screenshot-shot-list.md`, and four of the ten PNGs opened and
read at validation time: the Account, Season Stats, and Box Score phone-width
captures, plus the file listing for the remaining seven. Those PNGs were removed
before merge (MED-1) and are not part of this repository's history.

### Success metrics

| # | Metric | Result |
| --- | --- | --- |
| 1 | No horizontal overflow, 9 routes × 3 widths | **Met** — 27/27 |
| 2 | 100% AA contrast, both themes, ratios recorded | **Met** — 124/124 |
| 3 | Desktop parity ≥900px | **Met** — 220px sidebar, collapse to 36px functional |
| 4 | All destinations ≤2 taps at 375px | **Met** — 1 tap primary, 2 taps overflow |
| 5 | Clean build vs baseline | **Met** — no new errors or warnings |

---

**Validation Completed:** 2026-07-27 20:52 PT
**Validation Performed By:** Claude Opus 5 (claude-opus-5)
